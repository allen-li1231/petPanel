<!--page/userHome/pages/pets/petedit/gender/gender.wxml-->
  <view class="page" data-weui-theme="{{theme}}">
    <mp-cells title="">
      <mp-checkbox-group prop="radio" multi="{{false}}" bindchange="petGenderChange">
        <mp-checkbox wx:for="{{lst_pet_gender_condition}}" wx:key="value" label="{{item.pet_gender_condition}}" value="{{item.value}}"
          checked="{{item.checked}}"></mp-checkbox>
      </mp-checkbox-group>
    </mp-cells>
  </view>
